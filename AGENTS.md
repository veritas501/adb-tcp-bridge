## 语言规范

- 可以使用英文思考，但默认使用中文与用户沟通，除非用户另有要求。
- 代码标识符、内部错误信息、日志和非本地化字符串使用英文。
- 注释和开发文档可以使用中文。
- 用户可见文本遵循项目现有语言和国际化规范。

## 适用范围与优先级

- 规则发生冲突时，依次服从：用户明确要求、项目专属规则与配置、直接相关子系统的健康既有约定、本文件的通用默认。不得用通用偏好覆盖项目事实。
- `必须`、`不得` 表示无正当例外的约束；仅在上位规则冲突或客观条件无法满足时偏离，并说明原因与影响。`应`、`优先`、`原则上` 表示默认做法，可以根据项目事实调整并说明依据。

## 工作方式与设计

- 修改代码前，阅读相关模块的完整上下文，确认入口、调用方、下游依赖和测试，并参考 1～2 个最接近的现有实现。
- 优先沿用项目已有的目录、命名、错误处理、依赖管理和测试风格；若既有模式会明显降低代码健康度，应说明问题并在相关范围内修正。
- 参考的现有实现只有在系统角色、依赖与副作用边界、错误处理层级和测试契约可比时才能作为先例；仅靠文件名、命名或调用形状相似不足以照搬，否则会复制坏模式。
- 改动应聚焦当前需求、概念完整且可独立验证。允许进行承载需求所必需的局部重构，但不得混入无关的格式化、移动、重命名或重构，也不得以缩小 diff 为由保留明显不合理的结构。局部重构属于本改动的前提是：不做就会产生同一规则的第二条实现路径、给混合职责的 owner 增加职责、扩展参数团，或保留使目标行为不可测的边界；仅标准化无关代码、重命名大范围接口或清理需求未触及的模块不属于。
- 如果现有架构无法合理承载需求，或必须引入新的公共接口、依赖或架构模式，应先说明原因、影响范围和设计方案。
- 设计判断应基于明确需求、现有代码、业务不变量、失败模式以及测试或测量结果；先识别需求所属模块、相关状态、依赖和共同变化的行为，不以个人风格或“更工程化”为依据。
- 行为应放在其数据、策略和不变量最集中的 owner；判断归属时该 owner 应保持单一变更理由、调用方无需了解其内部拓扑、且一处业务规则改动只在一处完成；不为减少参数或文件数量而凭空创建 owner。
- 模块应职责清晰、依赖方向明确，避免循环依赖、跨层调用和隐式共享状态。抽象应匹配领域边界及已经明确的变化方向，不为假想需求预建框架或扩展点。
- 无状态、独立且可组合的计算优先使用函数；共享稳定依赖、状态、生命周期或业务不变量的一组行为可以形成内聚的 class，class 不以包含可变状态为必要条件。反测试：若一个 class 的所有方法都能移到模块作用域而不丢失不变量、生命周期、策略或 API 边界，则它只是形式上的 class，应改为函数。
- 不创建只做参数转发、没有边界价值的包装，也不提交缺少当前实际用途的公共抽象。
- 顺序敏感且共享上下文的完整流程，应优先让单个入口按业务顺序展示主要阶段，并用简短的块注释说明阶段目的、关键基数和顺序约束；不得仅为缩短函数而隐藏主流程。
- 是否提取 helper 不以代码行数或调用次数判断。某段逻辑具有独立复用价值、独立业务不变量、复杂算法或明确的副作用边界时应提取；仅做单步转发且不形成责任边界的包装应内联。

主流程内聚与 helper 提取的语言无关图解：

```text
# ── 规则：顺序敏感 + 共享上下文的流程 ──
#   单入口主流程 + 块注释标阶段；
#   helper 提取由「不变量/复用/边界/算法」触发，不由行数或调用次数触发。
#   语言无关（Go func / Python def / Java method / JS function 同构）。

# === 示例 A：顺序敏感主流程 ===

# 【反例】这些 helper 仅转发共享上下文、没有独立不变量或副作用边界，拆分只增加跳转
function checkout(order):
    lines = buildLines(order)
    lines = applyMemberDiscount(lines)
    lines = applyCoupon(lines)
    lines = applyPoints(lines)
    return buildResult(lines)

# 【正例】主流程内聚，块注释标阶段，阶段顺序即业务顺序（后段基数依赖前段产物）
function checkout(order):
    # 前置：快速失败
    if order.isEmpty(): fail("empty order")

    # ── 阶段 1：原价与活动优惠 ──
    # 活动后金额是后续所有比例分摊的基数
    subtotal = sum(line.price for line in order.lines)
    activity = applyActivity(subtotal)

    # ── 阶段 2：会员折扣 ──
    # 仅作用于活动后金额，避免与活动叠加放大
    afterMember = (subtotal - activity) * rate(order.tier)

    # ── 阶段 3：优惠券校验与分摊 ──
    if order.coupon.threshold > afterMember: fail("coupon threshold")
    coupon = min(order.coupon.discount, afterMember)

    # ── 阶段 4：积分、运费与最终金额 ──
    # 积分只抵商品金额，不抵运费
    total = afterMember - coupon - order.points / 100 + order.shipping
    return total

# === helper 提取判定 ===
# 满足任一条件才提取；只调用一次不等于应内联
function shouldExtract(block):
    return block.isReused              # 多处调用且职责稳定
        or block.hasInvariant          # 独立业务不变量，自带前后置条件
        or block.hasSideEffectBoundary # 纯 I/O / 加密 / 解析，隔离副作用
        or block.isComplexAlgorithm    # 自洽复杂计算
    # 不满足 → 内联 + 块注释；禁止仅以「缩短主函数」为由提取

# 【正例】该提取：封装多源 fallback，有独立不变量，即使只调用一次也形成真实边界
function getAuthServers(cfg):
    if cfg.host != "": return [cfg]             # 直配优先
    if cfg.root == "": fail("no server source") # 前置失败
    snap = collectServers(cfg.root)             # 安装目录扫描
    if snap.empty(): fail("no endpoints")
    return snap.activeEndpoints()
# 主流程只调用，不关心两套来源拓扑
servers = getAuthServers(cfg)

# 【反例】不该提取：只给单个表达式换名，没有独立职责或边界
function buildSubtotal(order):
    return sum(line.price for line in order.lines)
```

- 实现通用能力前，必须检查项目现有依赖和标准库，不重复实现已有可靠方案。标准库无法可靠满足复杂协议、加密、安全敏感解析、复杂数据格式、并发或跨平台兼容需求时，原则上采用成熟且维护活跃的开源库。只有不存在合适方案，或依赖成本明显高于自行实现时，才自行实现并说明取舍。
- 引入外部库前，应检查其许可证、维护状态、安全记录、API 稳定性、依赖体积和平台兼容性；不得仅为减少依赖数量而重复实现复杂的通用能力。
- 对人工维护的源码文件，若文件明显增长或已难以整体理解，应检查是否混合了多个职责；只有存在不同 owner、变更理由或依赖边界时才拆分，禁止按行数机械拆文件。生成文件和第三方代码不适用。

## 注释规范

- 注释和文档属于实现的一部分，应保存足够的局部上下文，使后来者无需了解当前对话也能理解职责、意图和关键约束。
- 代码主要表达“做什么”和“怎么做”；注释补充难以直接表达的设计意图、系统角色、业务不变量、调用时机、顺序要求、副作用和失败条件，不机械复述名称、参数或代码语句。
- 核心模块、公共接口和复杂逻辑应说明职责边界、使用场景和重要契约；块注释应概括阶段目的，不复述紧随其后的代码。
- 对不直观的算法、兼容处理、特殊分支和顺序约束，应解释采用该实现的原因及随意修改可能破坏的条件。描述调用关系时记录稳定的系统角色和契约，不维护易过时的调用方清单。
- 不记录临时开发过程、对话内容或显而易见的行为；修改代码时同步更新或删除失效、重复和误导性注释。

## 文档组织

- 文档分层：入口文档（如 README）只承担最短上手路径和地图导航；深入知识（架构、设计、专题）单独存放，入口用链接指向，不复制正文。
- 入口文档是地图不是百科：保证新访客快速理解项目用途、配置和运行方式，不堆砌 API 全表、长教程、设计长文或调研笔记。
- 模块说明就地维护：组件文档放在对应组件目录，跨模块总览用入口链接聚合，不制造第二份全文。
- 文档与代码同步：架构、公共接口、配置语义或运行命令变化时同步更新对应文档；不为记录开发过程创建文档。
- 不预建：不为对称或"以后可能有用"提前创建空目录或空文档；链接和目录树必须与仓库现状一致。
- 编辑自检：新增用户向说明优先放入口文档，过长再拆分；修改公开接口或配置时检查文档是否留下死链。

## 测试与验证规范

- 验证方式应匹配主要失败模式。新增可观察契约且既有测试未覆盖时，应在同一改动中补充能够保护该契约的测试；不为形式完整新增无法防止真实回归的测试。
- 纯内部重构使用既有行为测试和聚焦冒烟；新分支覆盖成功、边界和失败路径；状态或生命周期变更覆盖转换、不变量与清理路径；UI 和外部 IO 优先通过真实界面、适配器或传输验证可观察结果。
- 测试应验证外部可观察行为、业务不变量、边界条件和失败路径，不复制实现过程或只证明代码能够运行。
- 断言应能在目标行为被破坏时失败；避免脆弱、无意义或制造错误安全感的测试，并保持测试代码清晰可维护。
- 完成修改后，应运行项目已经配置且与改动相关的 formatter、lint、类型检查和测试，并实际运行或冒烟验证改动路径。项目未配置或检查不适用时，不为满足形式要求临时引入工具，但应说明采用的验证方式。
- 不得通过删除测试、降低检查标准、放宽断言或忽略错误来使验证通过。

## Python 代码规范

- 新建 Python 项目时，必须先检查 `uv` 是否可用；默认使用 `uv` 管理 Python 版本、虚拟环境、依赖和项目命令，并以 `pyproject.toml` 统一声明项目元数据、依赖及工具配置。只有用户明确要求、运行环境不支持或必须兼容既有工具链时才选择其他方案，并说明原因。
- 遵循项目现有风格和 Python 惯用写法，避免照搬其他语言的面向对象结构。
- `@classmethod` 应实际使用 `cls`，通常用于替代构造器或类级策略；`@staticmethod` 仅在行为明确属于该类型时使用，否则优先使用模块级函数。
- `dataclass` 用于表达数据结构，不使用普通 class 手工编写无额外逻辑的样板代码。
- 不为未来可能出现的实现提前创建抽象基类、Protocol 或工厂。
- 导入类型名时优先使用 `from module import Name`；普通函数和工具模块优先使用 `import module` 或 `from package import module`，调用时保留模块来源。

## 完成时的汇报

完成任务后简要说明：

- 修改了什么；
- 运行了哪些检查；
- 是否新增了公共抽象或外部依赖，以及新增原因。

没有新增时直接说明“无”，不需要展开。
